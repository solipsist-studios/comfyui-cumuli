[pytest]
# rootdir stays here: the pack root carries an __init__.py (ComfyUI imports the
# whole directory as a package) and pytest would try to collect it as a module.
addopts = --import-mode=importlib
